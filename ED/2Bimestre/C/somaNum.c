#include <stdio.h>

/* prints comentados p/ debug */

int main() {
    int A = 10, B = 2, C = 5, D = 3, reg = 0;
    
    reg = A + B;
    // printf ("%d\n", reg);

    reg = C + D;
    // printf ("%d", reg);

    return 0;
}